* How to Debug TIMEOUT
  * `journalctl -b -<n> ` check last boot `n`
  * search `fsck` (File system consistency check) from log to check if it was hard reboot
  * If there 
  * magic SysRq

Jenkins testflow reservation
